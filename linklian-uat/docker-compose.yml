services:
  # database:
  #   container_name: linklian-database-uat
  #   image: postgres:18
  #   restart: unless-stopped
  #   ports:
  #     - "5412:5432"
  #   environment:
  #     POSTGRES_USER: ${DB_USER}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #     POSTGRES_DB: ${DB_NAME}
  #   volumes:
  #     - ./databasepg:/var/lib/postgresql/data
  #   # healthcheck:
  #   #   test: ["CMD-SHELL", "pg_isready -U admin -d ${DB_NAME}"]
  #   #   interval: 10s
  #   #   timeout: 5s
  #   #   retries: 5
  #   networks:
  #     - linklian-net

  linklian-redis:
    image: redis:7-alpine
    container_name: linklian-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  linklian-upstash-redis:
    image: hiett/serverless-redis-http:latest
    container_name: linklian-upstash-redis
    ports:
      - "4001:80"
    environment:
      SRH_MODE: env
      SRH_TOKEN: 12345678
      SRH_CONNECTION_STRING: redis://linklian-redis:6379
    depends_on:
      linklian-redis:
        condition: service_healthy

volumes:
  redis-data:
    driver: local

networks:
  linklian-net:
    external: true